import ResourceManager from '@/components/dashboard/resource-manager'
import { orders } from '@/lib/admin-data'
export default function OrdersPage(){return <ResourceManager title="Orders" description="Track customer purchases, payments, preparation, and delivery." singular="order" initialRows={orders.map(item=>({...item}))} columns={[{key:'id',label:'Order'},{key:'customer',label:'Customer'},{key:'item',label:'Item'},{key:'date',label:'Date'},{key:'total',label:'Total'},{key:'status',label:'Status'}]} fields={[{key:'customer',label:'Customer name'},{key:'item',label:'Order item'},{key:'date',label:'Date'},{key:'total',label:'Total'}]} statuses={['Pending','Paid','Processing','Ready','Delivered','Cancelled']}/>} 

import ResourceManager from '@/components/dashboard/resource-manager'
import { bookings } from '@/lib/admin-data'
export default function BookingsPage(){return <ResourceManager title="Bookings" description="Plan events, confirm requirements, and keep every celebration on schedule." singular="booking" initialRows={bookings.map(item=>({...item}))} columns={[{key:'id',label:'Booking'},{key:'client',label:'Client'},{key:'event',label:'Event'},{key:'date',label:'Date'},{key:'guests',label:'Guests'},{key:'amount',label:'Value'},{key:'status',label:'Status'}]} fields={[{key:'client',label:'Client name'},{key:'event',label:'Event type'},{key:'date',label:'Event date',type:'date'},{key:'guests',label:'Guest count',type:'number'},{key:'amount',label:'Estimated value'}]} statuses={['Pending','In review','Confirmed','Completed','Cancelled']}/>} 

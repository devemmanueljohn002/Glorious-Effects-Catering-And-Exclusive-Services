'use client'

import { useState } from 'react'
import { Search, UserCog } from 'lucide-react'
import PageHeader from '@/components/dashboard/page-header'

type User={id:string;name:string;phone:string;roles:string[]}
export default function UsersPage(){const [users,setUsers]=useState<User[]>([]);const [query,setQuery]=useState('');const filtered=users.filter(user=>`${user.name} ${user.id}`.toLowerCase().includes(query.toLowerCase()));function addRole(id:string,role:string){if(!role)return;setUsers(current=>current.map(user=>user.id===id&&!user.roles.includes(role)?{...user,roles:[...user.roles,role]}:user))}function removeRole(id:string,role:string){setUsers(current=>current.map(user=>user.id===id?{...user,roles:user.roles.filter(item=>item!==role)}:user))}return <div className="dashboard-content"><PageHeader eyebrow="ACCESS CONTROL" title="Users & roles" description="Review registered profiles and grant role-based access."/><section className="panel management-panel"><div className="manager-toolbar"><label className="manager-search"><Search size={17}/><input value={query} onChange={event=>setQuery(event.target.value)} placeholder="Search by name or user ID…"/></label><span>{users.length} users</span></div><div className="table-wrap"><table><thead><tr><th>Name</th><th>Phone</th><th>Roles</th><th>Grant role</th></tr></thead><tbody>{filtered.map(user=><tr key={user.id}><td><strong>{user.name}</strong><small className="user-id">{user.id}</small></td><td>{user.phone||'—'}</td><td><div className="role-list">{user.roles.map(role=><span key={role}>{role}<button onClick={()=>removeRole(user.id,role)}>×</button></span>)}</div></td><td><select defaultValue="" onChange={event=>{addRole(user.id,event.target.value);event.target.value=''}}><option value="">Add role…</option><option>admin</option><option>instructor</option><option>customer</option></select></td></tr>)}</tbody></table>{filtered.length===0&&<div className="empty-table-state"><UserCog/><strong>No users found</strong><span>Registered profiles will appear here after authentication is connected.</span></div>}</div></section></div>}
